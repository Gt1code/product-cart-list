// import { foodData } from "./data";

const FoodBox = (props) => {
  return (
    <section>
      <img src={props.img} alt={props.alt} />
      <button></button>
    </section>
  );
};

export default FoodBox;
